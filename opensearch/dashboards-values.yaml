opensearchHosts: "http://opensearch-cluster-master.opensearch.svc.cluster.local:9200"

service:
  type: NodePort  # Use NodePort or ClusterIP as needed

extraEnvs:
  - name: OPENSEARCH_HOSTS
    value: "http://opensearch-cluster-master.opensearch.svc.cluster.local:9200"
